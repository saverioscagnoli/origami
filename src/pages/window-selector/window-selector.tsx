/* @refresh reload */
import { render } from "solid-js/web";

import "@style";

const WindowSelector = () => {
  return <div>winselector</div>;
};

render(() => <WindowSelector />, document.getElementById("root")!);
